describe('probar obtener puzzle', () => {
    test('probar con el mismo tipo', () => {

    });
    test('probar cambiar a siguiente tipo', () => {

    });
    test('probar sin un solo puzzle', () => {

    });
});

describe('probar logica de dificultad', () => {
    test("no subir nivel de dificulta si el puzzle anterior tomo mucho tiempo", () => {

    });
})